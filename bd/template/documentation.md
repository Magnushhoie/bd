Run bd to see the shared template menu:
.txt files open as menus
.sh and .py files are executed
.md files are viewed with less
Folders open a file picker; selected files are copied into the current directory
and existing files require overwrite confirmation